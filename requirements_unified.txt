# Unified Pipeline Requirements
# Install with: pip install -r requirements.txt

# ── Core PyTorch (install via conda or official wheel for CUDA match) ─────────
# torch>=2.3.0
# torchaudio>=2.3.0
# torchvision>=0.18.0

# ── Moshi / Mimi ──────────────────────────────────────────────────────────────
moshi==0.2.4
sphn>=0.1.4
sentencepiece>=0.1.99
safetensors>=0.4.0

# ── Bridge ────────────────────────────────────────────────────────────────────
transformers>=4.40.0
pyyaml>=6.0
numpy>=1.24.0
librosa>=0.10.0
pyworld>=0.3.4

# ── AvatarForcing / Wan ───────────────────────────────────────────────────────
omegaconf>=2.3.0
einops>=0.7.0
pillow>=10.0
decord>=0.6.0
opencv-python-headless>=4.8.0
pandas>=2.0
lmdb>=1.4.0

# ── Audio ─────────────────────────────────────────────────────────────────────
soundfile>=0.12.1

# ── HuggingFace & Diffusion ───────────────────────────────────────────────────
huggingface_hub>=0.23.0
transformers==4.41.2
accelerate==0.31.0
diffusers==0.30.0
peft==0.13.0
ftfy
easydict

# ── Async / Streaming ─────────────────────────────────────────────────────────
websockets>=12.0
nest-asyncio>=1.6.0

# ── Notebook ──────────────────────────────────────────────────────────────────
ipywidgets>=8.0
tqdm>=4.66
tensorboard>=2.14.0

# ── Optional ──────────────────────────────────────────────────────────────────
# None - we rely on PyTorch SDPA for stability over flash-attn.
